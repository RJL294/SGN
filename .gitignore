node_modules/
npm-debug.log*

# Note: data/archive.json IS committed — it's the accumulating news archive
# the daily refresh appends to (stories retire after ~3 months).

.DS_Store

# Built site — generated by `npm run build` and deployed by CI; not tracked.
dist/
